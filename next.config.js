/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "uz"], // Qo‘llab-quvvatlanadigan tillar
    defaultLocale: "en", // Default til
  },
};

module.exports = nextConfig;
